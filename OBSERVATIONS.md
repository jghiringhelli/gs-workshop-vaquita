# Session Observations — Participant P024

Fill this in during your last commit (when you get the 15-minute warning).
One sentence per question — no pressure to write more.

## What worked well?

Implementing the API in layers with tests and using the AI to iterate on failing checks felt smooth and productive.

## What slowed you down?

PowerShell quirks, the in-memory database resetting state, and fixing pre-push scoring issues took the most time.

## How did you handle git commits today?

told the AI

## Anything surprising?

It was surprising that the server was already running even when the terminal did not show obvious startup output at first.
