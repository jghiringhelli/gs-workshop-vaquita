NODE_ENV=development

# Database — file path for better-sqlite3
DATABASE_URL=./tanda.db

# JWT — REQUIRED, never hardcode in source
JWT_SECRET=change-me-to-a-long-random-secret

# Server
PORT=3000

# Business rules (configurable)
MAX_PARTICIPANTS=20
PENALTY_PERCENT=5

